# Matugen Hyprlock Colors
# Generated from: Null
# This file is managed by Matugen — do not edit manually.
#
# Dedicated template for hyprlock so we can have lock-specific color tweaks
# without polluting the main hyprland colors.

# ============================================
# Core Material You Colors (same as hyprland)
# ============================================

$background = rgba(181216ff)
$on_background = rgba(ecdfe5ff)

$primary = rgba(f5b2e2ff)
$on_primary = rgba(4e1e45ff)
$primary_container = rgba(68355dff)
$on_primary_container = rgba(ffd7f1ff)

$secondary = rgba(dcbed1ff)
$on_secondary = rgba(3e2a38ff)

$surface = rgba(181216ff)
$on_surface = rgba(ecdfe5ff)
$surface_variant = rgba(4f444aff)
$on_surface_variant = rgba(d2c2caff)

$surface_container = rgba(241e22ff)
$surface_container_high = rgba(2f282cff)
$surface_container_highest = rgba(3a3337ff)

$outline = rgba(9b8d94ff)
$outline_variant = rgba(4f444aff)

$error = rgba(ffb4abff)
$on_error = rgba(690005ff)

# Source color + tertiary (useful for accents)
$source_color = rgba(a78a9dff)
$icolor = $source_color

$tertiary = rgba(f5b9a3ff)
$on_tertiary = rgba(4c2618ff)
$tertiary_container = rgba(663c2cff)

# ============================================
# Useful Aliases (for compatibility)
# ============================================

$bg = $background
$fg = $on_background
$text = $on_surface
$accent = $primary
$accent2 = $secondary

# Hyprbars / common UI friendly names
$bg0 = $surface
$bg1 = $surface_container
$bg2 = $surface_container_high

# Legacy / compatibility variables
$grey0 = rgba(4f444aff)

# Some common accent names many configs expect
$red = $error
$green = $primary
$yellow = $secondary
$blue = $primary

# ============================================
# Extra convenience variables
# ============================================

$surface_dim = rgba(181216ff)
$surface_bright = rgba(3f373cff)

# ============================================
# Hyprlock-specific variables (easy to customize here)
# ============================================

# Slightly more transparent card background for the lock screen input
$lock_input_bg = rgba(2f282ccc)

# Stronger accent for the lock screen
$lock_accent = $primary
