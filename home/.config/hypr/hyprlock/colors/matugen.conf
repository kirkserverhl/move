# Matugen Hyprlock Colors
# Generated from: /home/kirk/Pictures/Wallpapers/cat-astronaut.png
# This file is managed by Matugen — do not edit manually.
#
# Dedicated template for hyprlock so we can have lock-specific color tweaks
# without polluting the main hyprland colors.

# ============================================
# Core Material You Colors (same as hyprland)
# ============================================

$background = rgba(141318ff)
$on_background = rgba(e5e1e9ff)

$primary = rgba(c7bfffff)
$on_primary = rgba(2f295fff)
$primary_container = rgba(463f77ff)
$on_primary_container = rgba(e4dfffff)

$secondary = rgba(c8c3dcff)
$on_secondary = rgba(312e41ff)

$surface = rgba(141318ff)
$on_surface = rgba(e5e1e9ff)
$surface_variant = rgba(47464fff)
$on_surface_variant = rgba(c9c5d0ff)

$surface_container = rgba(201f25ff)
$surface_container_high = rgba(2a292fff)
$surface_container_highest = rgba(35343aff)

$outline = rgba(928f99ff)
$outline_variant = rgba(47464fff)

$error = rgba(ffb4abff)
$on_error = rgba(690005ff)

# Source color + tertiary (useful for accents)
$source_color = rgba(8e8aa1ff)
$icolor = $source_color

$tertiary = rgba(ecb8ceff)
$on_tertiary = rgba(482537ff)
$tertiary_container = rgba(613b4dff)

# ============================================
# Useful Aliases (for compatibility)
# ============================================

$bg = $background
$fg = $on_background
$text = $on_surface
$accent = $primary
$accent2 = $secondary

# Hyprbars / common UI friendly names
$bg0 = $surface
$bg1 = $surface_container
$bg2 = $surface_container_high

# Legacy / compatibility variables
$grey0 = rgba(47464fff)

# Some common accent names many configs expect
$red = $error
$green = $primary
$yellow = $secondary
$blue = $primary

# ============================================
# Extra convenience variables
# ============================================

$surface_dim = rgba(141318ff)
$surface_bright = rgba(3a383eff)

# ============================================
# Hyprlock-specific variables (easy to customize here)
# ============================================

# Slightly more transparent card background for the lock screen input
$lock_input_bg = rgba(2a292fcc)

# Stronger accent for the lock screen
$lock_accent = $primary
