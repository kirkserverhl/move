/* Matugen palette / source-color chooser — fuzzel-matched */

configuration {
    show-icons:      true;
    icon-theme:      "Gruvbox Plus Dark";
    hover-select:    false;
    me-select-entry: "";
    me-accept-entry: "MousePrimary";
    cycle:           false;
}

@import "~/.config/rofi/shared/standard.rasi"
@import "~/.config/rofi/shared/fuzzel-chrome.rasi"

window {
    width:    420px;
    height:   380px;
    location: center;
    anchor:   center;
}

mainbox {
    spacing:  6px;
    padding:  10px;
    children: ["inputbar", "listview"];
}

inputbar {
    children: [ "prompt", "entry" ];
}

prompt {
    enabled:    true;
    text-color: @prompt-color;
    padding:    0 6px 0 0;
}

entry {
    placeholder: "Select option...";
}

listview {
    lines:        7;
    fixed-height: true;
    scrollbar:    true;
    cycle:        false;
    dynamic:      false;
    spacing:      3px;
    padding:      4px;
}

element {
    padding: 8px 12px;
}

element-icon {
    size:   28px;
    margin: 0 8px 0 2px;
    border-radius: 4px;
}